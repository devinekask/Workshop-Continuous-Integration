# This is a basic workflow to help you get started with Actions

name: Build images

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2
      
    - name: Use Node.js
      uses: actions/setup-node@v1
      with:
        node-version: '13.x'
      
    - name: Install dependencies
      run: yarn

    - name: Welke versie van node is dat hier eigenlijk?
      run: node -v

    - name: Map aanmaken voor de images
      run: mkdir images

    - name: generate all the images
      run: node build.js
    
    - name: Commit imges 
      run: |
        git config --local user.name "action"
        git add ./images
        git commit -m "Generated images"
        
    - name: Push changes # push the output folder to your repo
      uses: devinekask/techworkshoptest@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
